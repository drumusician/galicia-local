defmodule GaliciaLocal.Repo.Migrations.AddPhotoUrls do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:businesses) do
      add :photo_urls, {:array, :text}, default: []
    end
  end

  def down do
    alter table(:businesses) do
      remove :photo_urls
    end
  end
end
